
Метод stream() введено в Java 9.
Якщо значення присутнє, повертає Stream, що містить лише його
значення, інакше повертає порожній Stream.

До Java 9 не було способу перетворити Optional в одноелементний
потік, якщо Optional не порожній, або порожній потік, якщо Optional
порожній.


